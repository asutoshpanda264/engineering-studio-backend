package com.engineeringstudio.api.contribution;

/** One generic contribution type with a category, not three separate entities — see decisions.md. */
public enum ContributionCategory {
    QUESTION,
    POST,
    VLOG
}
